# webhook-app-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: webhook-app
  namespace: default
spec:
  selector:
    app: webhook-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080